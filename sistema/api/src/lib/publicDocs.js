/** Tipos de documento seguros para exposição pública (sem docs internos/sensíveis). */
const DOC_PUBLICOS = new Set(['planta', 'inteligencia', 'rig', 'avcb', 'convencao', 'iptu_doc']);

module.exports = { DOC_PUBLICOS };
